[package]
name = "tactician"
version = "0.0.1"
authors = [ "Scott Ostler" ]

[profile.release]
panic = "abort"

[profile.dev]
panic = "abort"

[dependencies]
getopts = "0.2.*"
itertools = "0.4.*"
lazy_static = "0.2.*"
rand = "0.3.*"
